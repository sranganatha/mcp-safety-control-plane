[project]
name = "mcp-safety-control-plane"
version = "0.1.0"
description = "Local reference implementation for governed MCP tool access"
requires-python = ">=3.12"
dependencies = ["mcp==2.1.1"]

[tool.setuptools]
packages = ["mcp_control_plane"]
